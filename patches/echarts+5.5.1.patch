diff --git a/node_modules/echarts/lib/chart/bar/BarView.js b/node_modules/echarts/lib/chart/bar/BarView.js
index ad07d5a..50c4ed2 100644
--- a/node_modules/echarts/lib/chart/bar/BarView.js
+++ b/node_modules/echarts/lib/chart/bar/BarView.js
@@ -62,13 +62,22 @@ import { getSectorCornerRadius } from '../helper/sectorHelper.js';
 var mathMax = Math.max;
 var mathMin = Math.min;
 function getClipArea(coord, data) {
+  if (!coord || !coord.getBaseAxis) {
+    return null;
+  }
   var coordSysClipArea = coord.getArea && coord.getArea();
   if (isCoordinateSystemType(coord, 'cartesian2d')) {
     var baseAxis = coord.getBaseAxis();
+    if (!baseAxis) {
+      return coordSysClipArea;
+    }
     // When boundaryGap is false or using time axis. bar may exceed the grid.
     // We should not clip this part.
     // See test/bar2.html
     if (baseAxis.type !== 'category' || !baseAxis.onBand) {
+      if (!coordSysClipArea) {
+        return null;
+      }
       var expandWidth = data.getLayout('bandWidth');
       if (baseAxis.isHorizontal()) {
         coordSysClipArea.x -= expandWidth;
@@ -130,7 +139,13 @@ var BarView = /** @class */function (_super) {
     var data = seriesModel.getData();
     var oldData = this._data;
     var coord = seriesModel.coordinateSystem;
+    if (!coord) {
+      return;
+    }
     var baseAxis = coord.getBaseAxis();
+    if (!baseAxis) {
+      return;
+    }
     var isHorizontalOrRadial;
     if (coord.type === 'cartesian2d') {
       isHorizontalOrRadial = baseAxis.isHorizontal();
@@ -181,7 +196,7 @@ var BarView = /** @class */function (_super) {
         return;
       }
       var isClipped = false;
-      if (needsClip) {
+      if (needsClip && coordSysClipArea) {
         // Clip will modify the layout params.
         // And return a boolean to determine if the shape are fully clipped.
         isClipped = clip[coord.type](coordSysClipArea, layout);
@@ -243,7 +258,7 @@ var BarView = /** @class */function (_super) {
         return;
       }
       var isClipped = false;
-      if (needsClip) {
+      if (needsClip && coordSysClipArea) {
         isClipped = clip[coord.type](coordSysClipArea, layout);
         if (isClipped) {
           group.remove(el);
diff --git a/node_modules/echarts/lib/layout/barPolar.js b/node_modules/echarts/lib/layout/barPolar.js
index 9597bc2..edc0170 100644
--- a/node_modules/echarts/lib/layout/barPolar.js
+++ b/node_modules/echarts/lib/layout/barPolar.js
@@ -51,21 +51,31 @@ function getAxisKey(polar, axis) {
   return axis.dim + polar.model.componentIndex;
 }
 function barLayoutPolar(seriesType, ecModel, api) {
+  if (!ecModel) {
+    return;
+  }
   var lastStackCoords = {};
   var barWidthAndOffset = calRadialBar(zrUtil.filter(ecModel.getSeriesByType(seriesType), function (seriesModel) {
-    return !ecModel.isSeriesFiltered(seriesModel) && seriesModel.coordinateSystem && seriesModel.coordinateSystem.type === 'polar';
+    return !ecModel.isSeriesFiltered(seriesModel) && seriesModel && seriesModel.coordinateSystem && seriesModel.coordinateSystem.type === 'polar';
   }));
   ecModel.eachSeriesByType(seriesType, function (seriesModel) {
     // Check series coordinate, do layout for polar only
-    if (seriesModel.coordinateSystem.type !== 'polar') {
+    if (!seriesModel || !seriesModel.coordinateSystem || seriesModel.coordinateSystem.type !== 'polar') {
       return;
     }
     var data = seriesModel.getData();
     var polar = seriesModel.coordinateSystem;
     var baseAxis = polar.getBaseAxis();
+    if (!baseAxis) {
+      return;
+    }
     var axisKey = getAxisKey(polar, baseAxis);
     var stackId = getSeriesStackId(seriesModel);
-    var columnLayoutInfo = barWidthAndOffset[axisKey][stackId];
+    var axisBuckets = barWidthAndOffset[axisKey];
+    var columnLayoutInfo = axisBuckets && axisBuckets[stackId];
+    if (!columnLayoutInfo) {
+      return;
+    }
     var columnOffset = columnLayoutInfo.offset;
     var columnWidth = columnLayoutInfo.width;
     var valueAxis = polar.getOtherAxis(baseAxis);
@@ -166,9 +176,15 @@ function calRadialBar(barSeries) {
   // Columns info on each category axis. Key is polar name
   var columnsMap = {};
   zrUtil.each(barSeries, function (seriesModel, idx) {
+    if (!seriesModel || !seriesModel.coordinateSystem || seriesModel.coordinateSystem.type !== 'polar') {
+      return;
+    }
     var data = seriesModel.getData();
     var polar = seriesModel.coordinateSystem;
     var baseAxis = polar.getBaseAxis();
+    if (!baseAxis) {
+      return;
+    }
     var axisKey = getAxisKey(polar, baseAxis);
     var axisExtent = baseAxis.getExtent();
     var bandWidth = baseAxis.type === 'category' ? baseAxis.getBandWidth() : Math.abs(axisExtent[1] - axisExtent[0]) / data.count();
diff --git a/node_modules/echarts/lib/processor/dataSample.js b/node_modules/echarts/lib/processor/dataSample.js
index d541285..984ca79 100644
--- a/node_modules/echarts/lib/processor/dataSample.js
+++ b/node_modules/echarts/lib/processor/dataSample.js
@@ -112,7 +112,7 @@ export default function dataSample(seriesType) {
       var coordSys = seriesModel.coordinateSystem;
       var count = data.count();
       // Only cartesian2d support down sampling. Disable it when there is few data.
-      if (count > 10 && coordSys.type === 'cartesian2d' && sampling) {
+      if (count > 10 && coordSys && coordSys.type === 'cartesian2d' && sampling) {
         var baseAxis = coordSys.getBaseAxis();
         var valueAxis = coordSys.getOtherAxis(baseAxis);
         var extent = baseAxis.getExtent();
